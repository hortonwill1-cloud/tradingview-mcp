services:
  - type: web
    name: tradingview-mcp
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /mcp
    envVars:
      - key: API_KEY
        sync: false
      - key: HOST
        value: 0.0.0.0
